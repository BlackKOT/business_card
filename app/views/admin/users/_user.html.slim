div.text-center ng-repeat="user in users"
  img.center-block src="{{ user.avatar_url }}" class="img-circle img-responsive img-center fix-size"
  = link_to '{{ user.full_name }}', URI::decode_www_form_component(edit_admin_user_path(id: '{{ user.id }}'))
